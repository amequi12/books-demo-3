<div class="slide-out-bg"></div>

<div class="slide-out">
  <div class="slide-out-card">
    {{#link-to "book.index" class="btn-back"}}
    <span class="sr-only">{{t "back"}}</span>
    {{/link-to}}

    <div class="slide-out-heading">
      <div class="title">
        <h3>{{model.bookName}}</h3>

        {{#if (await (can "edit record" model))}}
          <div class="actions">
            {{#link-to "book.edit" model.id class="btn-pop"}}{{t "edit"}}{{/link-to}}

            <button class="btn-recessed" onclick={{action "deleteBook" model}}>{{t "delete"}}</button>
          </div>
        {{/if}}
      </div>
    </div>

    <div class="slide-out-content">
      <div class="field">
        <label>{{t "book.attrs.bookName"}}</label>
        {{model.bookName}}
      </div>

      <div class="field">
        <label>{{t "book.attrs.author"}}</label>
        {{model.author}}
      </div>

      <div class="field">
        <label>{{t "book.attrs.pagesNumber"}}</label>
        {{model.pagesNumber}}
      </div>

      <div class="field">
        <label>{{t "book.attrs.cover"}}</label>
        {{model.cover}}
      </div>

      <div class="field">
        <label>{{t "book.attrs.tags"}}</label>
        <span>{{#each model.tags as |tag|}}
          {{#link-to "book.search-by-tag" tag class="btn-pop"}}
          {{tag}}
          {{/link-to}}
          {{/each}}
        </span>
      </div>
      <div class="field">
        <label>{{t "book.reports"}}</label>
        <ul class="collection">
          {{#each model.reports as |report|}}
          <li>
            {{#link-to "report.detail" report.id}}
            {{report.reportDate}}
            {{/link-to}}
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>