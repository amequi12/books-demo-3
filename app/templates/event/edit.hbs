<div class="slide-out-bg"></div>
<div class="slide-out">
  <div class="slide-out-card">
    {{#link-to "event" class="btn-back"}}
      <span class="sr-only">{{t "back"}}</span>
    {{/link-to}}

    <div class="slide-out-heading">
      <div class="title">
        <h3>{{t "event.edit"}}</h3>
      </div>
    </div>

    <div class="slide-out-content">
        {{event-form event=model onsubmit=(action "editEvent")}}
    </div>
  </div>
</div>